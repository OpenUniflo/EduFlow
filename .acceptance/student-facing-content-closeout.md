# Student-Facing Content — Acceptance

Date: 2026-09-10 Asia/Shanghai. One Agent. Scope: global authoring contract and `ai-agents-in-depth` content only.

## A. Baseline

Branch `feat/gold-course-learning-loop`; starting local/fetched remote HEAD `700853d86a90188ad5bf7c2e950729f87ab3e9cb`; clean tree, no existing user edits. Ahead 17 / behind 0 versus `origin/prototype`. Hosted project `uyljtdbvlivxniililay`; 46 migrations matched through `20260909140829_micro_rich_text_observable_content`. Target counts confirmed: 12 Published Micro / 65 Steps. Whole Hosted: 19 Paths / 103 Steps. Baseline source/Hosted parity includes full title/kind/body/feedback/interaction and Unit/Step positions.

Existing runtime authority is normalized Hosted Micro tables. Reviewed offline authority is `data/gold-courses/ai-agents-in-depth-rich-content.json`, generated by `scripts/dev/generate-rich-micro-migration.ts` into a forward migration. Existing revision contract increments each changed Path once, guards Course/Knowledge/Path identities, published status, revision, Unit/Step sets and positions, and verifies replay instead of overwriting newer edits. All 12 Paths had at least a source locator to remove.

Baseline typecheck, lint, 83 test files / 518 tests, production build, generated Micro verifier, 23 Micro V2 probes/H5P replay, learning-loop, Local backend, DB lint, Vercel build and client-secret audit passed. Existing CSS `*width` and large-chunk warnings remain. Latest pre-task Preview was `edu-flow-13h3wxeh0-july-nanas-projects.vercel.app`, rather than the older URL in the prior ledger; actual environment was used.

## B. Global rule

[Teaching Pattern Library](../docs/TEACHING_PATTERN_LIBRARY.md#student-facing-content-rule) owns the global rule; [Rich Text Content](../docs/RICH_TEXT_CONTENT.md) references it and specifies minimal empty-structure cleanup. Teaching includes explanations, examples, comparisons, processes, tasks, feedback and Summary. Source records remain in Material/provenance; navigation remains in Course Shell; completion/mastery remains in Learning State. All student-visible fields require semantic review. Example scope, non-live data, preset traces and factual uncertainty remain.

## C–E. Audit and content

[Full 65-Step audit and exact edits](student-facing-content-audit.md). Counts are distinct Steps within each overlapping category:

| Category | Before | Final |
| --- | ---: | ---: |
| Removed — Provenance | 12 | 0 |
| Rewritten — Authoring Metadata | 14, including 11 adaptation-label Steps | 0 |
| Combined source/authoring candidates | 26 | 0 |
| Removed — Route Coupling | 19 | 0 |
| Removed — Product State / capability disclaimer | 2 | 0 |
| Unique affected Steps | 36 | 36 bodies changed; 29 unchanged |

Least Privilege states credential limits directly and loses source, course-route and mastery disclaimers. Message Context and Agent Loop summaries retain their information-chain teaching without PDF locators. Observation/Action Spaces, Model Selection, Guardrail and Sandbox use direct scenarios. Harness and LLM Agent Architecture retain bounded definitions and role relationships. Workflow preserves its example-only booking sequence. Agentic Workflow retains example-specific checkpoints and approval boundaries. ReAct retains preset traces, user authorization, feedback and limitations of the failure demonstration.

Thirteen caveat-bearing Steps are explicitly enumerated in the audit, including non-real-time weather, non-live inventory, single-tool versus parallel ordering, preset ReAct traces, non-universal fault behavior, Workflow example boundaries and the rewritten hybrid-flow scope. “来源” describing message origin and “下一步” describing an Agent decision are retained. No global semantic classifier or Publish gate was introduced.

## F. Technical verification

| Command / check | Baseline | Final |
| --- | --- | --- |
| `pnpm typecheck` | PASS | PASS |
| `pnpm lint` | PASS | PASS |
| `pnpm test` | PASS, 518 tests | PASS, 520 tests / 83 files |
| `pnpm build` | PASS | PASS |
| `pnpm verify:micro:generated` | PASS | PASS |
| `pnpm exec tsx --env-file=.env.local scripts/verify-micro-v2.ts` | PASS | PASS, 23 SQL/TS probes, isolated history replay and H5P stability |
| `pnpm verify:learning-loop:local` | PASS | PASS |
| `pnpm verify:backend:local` | PASS | PASS |
| `pnpm exec supabase db lint --local --level warning` | PASS | PASS |
| `pnpm exec vercel build` | PASS | PASS, 12 generated Functions |
| `pnpm audit:client-secrets` | PASS | PASS |
| `pnpm exec vitest run tests/agentRichContent.test.tsx tests/richText.test.tsx` | included in full suite | PASS, 8 tests |
| Local forward migration + exact replay | — | PASS; 53 protected fingerprints stable on replay |
| Hosted native interaction validation | — | PASS, 23 interactions / 0 invalid |
| Authenticated Preview API parity | — | PASS, all 65 Steps and feedback/interactions |
| `git diff --check` | clean baseline | PASS |

The two new tests protect only this reviewed course's explicit unwanted phrases and important caveats, including nested interaction content and text split across rich-text nodes. They do not decide learning semantics or participate in Publish.

## G. Browser and deployment

Accepted Preview: `https://edu-flow-ht7yk8dro-july-nanas-projects.vercel.app`. Remote build READY; deployment inspection lists exactly 12 lambda outputs. Ordinary-user login and Health/Courses/Knowledge/Micro/Progress return 200; public Supabase URL resolves to the verified Hosted project. Existing ordinary learner already completed all 12 Paths; review uses the real authenticated session, real API definitions and existing review behavior.

[Browser evidence](student-facing-content-browser.json): **12 Micro / 65 Steps × 1440 and 390 = 130 viewport checks**, all PASS. Required six: Least Privilege, LLM Message Context, Agent Loop, Observation and Action Spaces, Agent Harness and Workflow. Additional six: LLM Agent Architecture, ReAct, Guardrail, Sandbox, Agentic Workflow and Model Selection. Verified Summary, rich-text rendering, no document horizontal overflow, CTA hit testing, current-step Why explanations, choice/categorize/keyboard ordering, flow execution/connection repair and Sandbox wrong-answer/retry. Zero page errors and zero application writes in the accepted runs. Actual mobile viewport places EduFlow Assistant below the card, clear of teaching and CTA; the black Preview toolbar visible in element screenshots is not EduFlow Assistant. No product UI change was needed.

An initial prebuilt Preview (`edu-flow-mfmd6jg9j-july-nanas-projects.vercel.app`) was READY but failed browser initialization: cached `.vercel/.env.preview.local` contained redacted placeholders. It was rejected. A remote Vercel build using actual hosted environment configuration fixed deployment without changing code or environment authority. An API smoke attempted while that remote deployment was still building was repeated after READY and passed.

The browser harness's Node API request timed out outside the browser proxy; it now uses real in-page fetch. Its literal “下一节” candidate scan initially matched the connection instruction “下一节点”; semantic review retained the legitimate graph instruction and narrowed only this audit check. These were acceptance-harness issues, not product defects.

## H. Hosted integrity

Applied forward-only migration `20260909194539_student_facing_agent_content.sql`, generated by the existing generator and tested locally first. Hosted dry-run listed only this migration, no seeds or roles; application and migration history confirmed. Schema DDL, new runtime types, Rich Text infrastructure edits, Navigation and learner-state code changes all equal zero.

Final revisions: CTX01/R10/RT01 **5**; A02/S03 **4**; AGC01/AGC02/AGC03/H02/S01/S02/WF05 **3**. Course/Knowledge/Path/Unit/Step IDs and positions remain stable. All Step fields except `content` exactly equal baseline, including interactions, feedback, title/kind, transition and timestamps. Path changes are revision-only. Material, segments, mappings and provenance remain untouched.

Protected fingerprints cover 49 complete public business tables, non-target Paths/Steps, target Path structure excluding revision, and target Step structure excluding editable content fields. Whole-row interaction/feedback equality is checked separately. No Hosted reset, seed, history rewrite or new test account occurred. Local learning-loop/backend regression exercises Local-only learner data; Hosted history is separately fingerprinted.

[Before/after integrity evidence](student-facing-content-integrity.json): **53/53 checks unchanged**, repeated after migration and after all browser reviews. Hashes use MD5 of concatenated JSONB rows in deterministic JSONB-text order (Micro projections use stable ID order); empty sets use an empty-string hash. This covers Course/Chapter/Lesson, Knowledge, Material/mappings, Assignment, learner history, attempts, performance, Evidence, State, Navigation, membership and all non-target Micro content/revisions. Detailed raw definitions, screenshots, scripts and logs remain outside Git under `/tmp/eduflow-student-content`; credential-derived browser storage is private and excluded.

## I. Git

Only this task's content, forward migration, two existing global documents, course-content tests and acceptance evidence are included. Commit/push stays on `feat/gold-course-learning-loop`. No merge to `prototype`.

## J. Deferred

Full Publish Validator, authoring editor, and other-course content cleanup/migration.
